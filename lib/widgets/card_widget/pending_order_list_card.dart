import 'package:flutter/material.dart';

class PendingOrderListCardWidget extends StatelessWidget {
  const PendingOrderListCardWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return const Scaffold();
  }
}
